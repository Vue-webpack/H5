// 获取商场id
export function mallId (state) {
  return state.common.mallId
}

// 获取用户id
export function unionId ( state ) {
  return state.common.unionId
}

// 获取用户id
export function userId ( state ) {
  return state.common.userId
}

// 获取用户信息
export function userInfo ( state ) {
  return state.common.userInfo
}

// 微信配置
export function wxConfig ( state ) {
  return state.common.wxConfig
}

// 获取loading状态
export function ajaxLoading ( state ) {
  return state.common.ajaxLoading
}


